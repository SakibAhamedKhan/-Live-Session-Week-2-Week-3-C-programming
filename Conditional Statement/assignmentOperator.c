#include <stdio.h>
int main()
{
    int a = 5, c=1;

    

    
    
    
    c -= a;     // c is -4 c=c-a
    printf("Amar biyogfol hocce c = %d\n");
    
    
    // c *= a;     // c is  c = c*a
    // printf("c = %d\n", c);
    
    
    // c /= a;     // c is   c = c/a
    // printf("c = %d\n", c);
    
    
    // c %= a;     // c = c%a
    // printf("c = %d\n", c);

    return 0;
}